apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music-claim
  namespace: musicplayer
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: linode-block-storage-retain
  selector:
    matchLabels:
      type: "music"